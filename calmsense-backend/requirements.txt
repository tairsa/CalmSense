fastapi
uvicorn[standard]

# Storage: Supabase (Postgres) is the primary store; the JSON file is a
# fallback. python-dotenv loads SUPABASE_URL / SUPABASE_KEY from a local .env.
supabase
python-dotenv

# Admin app auth. PyJWT is pure-Python (no native build on Windows). Password
# hashing uses the stdlib (hashlib.pbkdf2_hmac), so no bcrypt/passlib needed.
pyjwt

# ML training pipeline (used by ml/generate_data.py and ml/train_model.py).
# The runtime API does not import these — it only reads ml/model_weights.json,
# so a deployment that never retrains can skip these. Listed here for the
# developer-machine setup so `pip install -r requirements.txt` is one command.
scikit-learn
pandas
numpy
